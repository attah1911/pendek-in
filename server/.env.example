DATABASE_URL=
DIRECT_URL=
REDIS_URL=
JWT_SECRET=
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=12
IP_HASH_SALT=
# optional — 'none' only for a cross-domain client/API deploy (also forces Secure cookies)
COOKIE_SAMESITE=strict
CLIENT_ORIGIN=http://localhost:5173
# optional — origin shown in generated short links; empty = derived from the request
SHORT_URL_BASE=
PORT=3000
NODE_ENV=development
